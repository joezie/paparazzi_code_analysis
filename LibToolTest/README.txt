LibToolAnalysis.cpp: just a test using libTool

libToolTestCmd.sh: a script just for test

1. Run "make" to generate LibToolAnalysis.
   Before running "make", you need to modify "LLVM_SRC_PATH" & "LLVM_BUILD_PATH" in Makefile. Please follow the instruction in Makefile.

2. Run "./LibToolAnalysis <cfile>"
